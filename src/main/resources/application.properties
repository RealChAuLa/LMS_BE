# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://dpg-d0kcq2nfte5s738j8c80-a.oregon-postgres.render.com:5432/lms_d2v1
spring.datasource.username=lms_d2v1_user
spring.datasource.password=caw5FxnzaYRFsBREBnK6NoYriMSrpDdN
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Properties - CORRECTED DIALECT
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Or for newer versions of Hibernate, you can use:
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL10Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Connection Pool Properties
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000

# For Spring Boot 2.6.0 and above, to avoid conflicts with Springfox
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

server.port=${PORT:8080}